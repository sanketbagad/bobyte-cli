{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate --schema=server/prisma/schema.prisma && cd server && npm run build",
  "outputDirectory": "server/dist",
  "installCommand": "npm install"
}
